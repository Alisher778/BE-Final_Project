<%= form_for(@news_page) do |f| %>
  <% if @news_page.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@news_page.errors.count, "error") %> prohibited this news_page from being saved:</h2>

      <ul>
      <% @news_page.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="field">
    <%= f.file_field :image %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
